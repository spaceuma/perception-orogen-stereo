# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(dense_stereoTaskLib)
ADD_LIBRARY(${DENSE_STEREO_TASKLIB_NAME} SHARED 
    ${DENSE_STEREO_TASKLIB_SOURCES})

# use sse3 instruction set
SET(CMAKE_CXX_FLAGS "-msse3")

TARGET_LINK_LIBRARIES(${DENSE_STEREO_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${DENSE_STEREO_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${DENSE_STEREO_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${DENSE_STEREO_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${DENSE_STEREO_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${DENSE_STEREO_TASKLIB_HEADERS}
    DESTINATION include/orocos/dense_stereo)

