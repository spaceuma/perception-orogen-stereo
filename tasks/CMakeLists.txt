# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(stereoTaskLib)
ADD_LIBRARY(${STEREO_TASKLIB_NAME} SHARED 
    ${STEREO_TASKLIB_SOURCES})

add_dependencies(${STEREO_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${STEREO_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PCL_LIBRARIES}
    ${STEREO_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${STEREO_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${STEREO_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${STEREO_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${STEREO_TASKLIB_HEADERS}
    DESTINATION include/orocos/stereo)

